#include <iostream>
#include <math.h>
using namespace std;
// long long is annoying to type, so ll
// same as typedef long long ll;
using ll = long long;

int main()
{
    cout << floor(3.3) << endl;
    int a = 4.4, b = 3;
    // does floor division
    cout << a / b << endl;

    //modular arithmatec: using mod to do math
    // a [triple equal sign] b (mod n) -> a and b have the same remainder after dividing by n

    // euclidean algorithm: finds gcd

    // find primes: wieve of eratosthenes
    const int N = 1000000;
    //bitset<N + 1> prime;

    //sum of first n numbers = n(n+1)/2
}