#include <iostream>
using namespace std;

int main()
{
    // Hashing is compressed way of storing data
    // (ax^4 + bx^3 + cx^2 + dx^1 + ex^0) % M

    // Prefix sum array: Queries sum in a range
}