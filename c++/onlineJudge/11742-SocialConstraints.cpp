#include <stdio.h>

int main()
{
    int n, m;
    int a, b, c;
    int nSol = 0;

    while (scanf("%d %d", &n, &m), n, m)
    {
        for (int i = 0; i < m; i++)
        {
            scanf("%d %d %d", &a, &b, &c);
        }
    }
}