#include <bits/stdc++.h>

using namespace std;

#define pb push_back
#define INF 2e14

typedef long long ll;

const int mxN = 1e5;

int main()
{
    int arr[1];
    memset(arr, 0x3f3f3f3f, sizeof(arr));
    cout << arr[0] << '\n';
    arr[0] = 0;
    cout << arr[0] << '\n';
    memset(arr, 0x3f, sizeof(arr));
    cout << arr[0] << '\n';
}