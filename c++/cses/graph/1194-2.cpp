#include <bits/stdc++.h>

using namespace std;

typedef long long ll;

const int mxN = 1e3, mxM = 1e3, M = 1e9 + 7;

string s[mxN + 1];
bool vis[mxN + 1][mxN + 1];
int dist[mxN + 1][mxN + 1];

int main()
{
    int n, m;
    cin >> n >> m;
    for (int i = 0; i < n; i++)
    {
        cin >> s[i];
    }
}